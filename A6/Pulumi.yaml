name: A6-Budgets
description: 'Budgets: Setup a cost limit'
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
